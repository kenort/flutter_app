name: prueba
description: Una aplicación para enviar y recibir datos desde Google Sheets.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0 # Para manejar solicitudes HTTP
  signature: ^5.5.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true