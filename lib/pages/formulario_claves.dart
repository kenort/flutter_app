import 'package:flutter/material.dart';

class FormularioPage3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Asignación Claves")),
      body: Center(child: Text("Formato Asignación de Claves")),
    );
  }
}
