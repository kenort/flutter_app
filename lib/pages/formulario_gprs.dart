import 'package:flutter/material.dart';

class FormularioPage5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Entrega GPRS")),
      body: Center(child: Text("Form entrega GPRS")),
    );
  }
}
