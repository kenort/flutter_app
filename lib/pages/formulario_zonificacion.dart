import 'package:flutter/material.dart';

class FormularioPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Zonificación")),
      body: Center(child: Text("Formulario de Zonificación")),
    );
  }
}
