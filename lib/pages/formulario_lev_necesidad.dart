import 'package:flutter/material.dart';

class FormularioPage4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Levantamiento de Necesidad")),
      body: Center(child: Text("Formulario Levantamiento Necesidad")),
    );
  }
}
